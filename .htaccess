Options -Indexes

<IfModule mod_rewrite.c>
RewriteEngine On

# أي طلب لملف حقيقي (صور، css، js) يمر عادي
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# أي طلب آخر يمر إلى gate.php للحماية
RewriteRule ^ gate.php [QSA,L]
</IfModule>
